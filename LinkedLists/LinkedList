import LinkedListNode from './Node'

class LinkedList {
  constructor() {
  }
};


/* 
TODO: 
  Node class with data and next 
  LinkedList class with head and tail
    with methods: 
      prepend(nodeToHead)
      append(nodeToTail)
      delete(node)
      find(node)
      deleteTail()
      deletHead()
*/